Weekly Accomplishments
The objective was to review all our past code and reorganize our documentation and repository. 

We assigned our code among ourselves for the review and created a codeReview.md to hold all the reviews we made on each other's code.

Changes made:

We optimized imports to follow the PEP8 recommended naming convention for our function, variable and constant naming.

We added docstrings to our functions.

We separated code blocks which we considered different to different Notebook cells.

We added more markdown headers and code comments to improve readability and clarity.


Challenges: Our main challenge this week was not being able to work on the decision tree because we found out we were meant to have a code review for each code. This also should have been done every week to make sure our code works on every machine and that it meets the coding standard.

Tasks to Accomplish for Week 9.

In week 9 we will create decision trees to predict disease outputs via multi-label classification. There will be 3 outputs:

[ ] Decision Tree using the Images exclusively

[ ] Decision Tree using the Images + Age Column

[ ] Decision Tree using the CSV file and Diagnosis Columns

We will be using 3 different decision tree algorithms in order to decide which one gives the best result.

There will also be a code review for all decision tree algorithms used.

*Part of this process will require us to determine if binning the ages or using them as scaled integers is better.
